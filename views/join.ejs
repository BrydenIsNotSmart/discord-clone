<% include partials/header %>

    <!-- Page Content -->
    <div class="invitation">
        <div class="invitation-header">
            You've been invited to join
        </div>
        <div class="invitation-info">
            <div class="col">
                <div class="group-avatar"><img src="<%= guild.guild_picture %>" /></div>
            </div>
            <div class="col">
                <h6 class="mb-10"><%= guild.guild_name %></h6>
                <ul class="group-stats">
                    <li><%= guild.participant.length %> users registered</li>
                    <% var count = 0; %>
                    <% guild.participant.forEach(function(participant){ %>
                        <% if(participant.online === true){ %>
                            <% count++ ; %>
                        <% } %>
                    <% }); %>
                    <li><%= count %> online users</li>
                </ul>
                <a href="/" class="decline link-underline">Decline Invitation</a>
            </div>
        </div>
        <div class="invitation-join">
                <form action="/guild/<%= guild._id %>/join" method="POST">
                    <button type="submit" class="btn btn-block btn-primary">Join</button>
                </form> 
        </div>
    </div>


<% include partials/footer %>

